<style>
.my-text {
background-color: #f5f5f5;
padding: 10px;
margin-left: 30%;
}
</style>

<div class="my-text">
JavaScriptです。次のようなコードがあります。

```
   let books = [];
   let jsbook = {};

   jsbook['title'] = 'いやいやえん';
   jsbook['price'] = 1400;
   jsbook['stock'] = 3;
   books.push(jsbook);

   jsbook['title'] = 'エルマーと竜';
   jsbook['price'] = 2500;
   jsbook['stock'] = 4;
   books.push(jsbook);
   
   console.log(books);
```
最後に 配列books をみてみると、最初にpushした内容が、
次にpushした内容に変更され、結局同じ内容が2つ配列に格納されています。
books.push()でプッシュされたのは jsbookオブジェクトの参照であって、
そのときの値ではないということですか。
</div>

<!-- 修正時刻: Sat 2026/03/07 05:44:19 -->
