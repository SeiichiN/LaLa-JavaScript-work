(1) setTimeoutを使って簡単なプログラムを書く。

   a. 3秒後に div#output に「ヤッホー」と表示するプログラム

   b. 1秒ごとにサイコロをふるプログラム。
      (1～6をランダムに出力する)
      最初はコンソールに出力して、それができれば、
      次はbodyの中に出力する。

   c. 1秒ごとにカウントダウンするプログラム。
      最後0になったら、'GO!'と表示する。
      (秒数はグローバル変数にしておいた方がやりやすい)
      
      